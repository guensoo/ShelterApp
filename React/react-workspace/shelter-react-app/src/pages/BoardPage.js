import { Box, Typography } from "@mui/material";
const BoardPage = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h5">자유게시판</Typography>
    <Typography>게시판 목록/글쓰기 UI가 여기에!</Typography>
  </Box>
);
export default BoardPage;
